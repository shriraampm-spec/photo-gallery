name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies (Root)
        run: npm install

      - name: Backend - Install and Test
        run: |
          cd backend
          npm install
          if [ -n "$MONGO_URI" ]; then
            npm test
          else
            echo "Skipping backend tests as MONGO_URI is not set."
          fi
        env:
          MONGO_URI: ${{ secrets.MONGO_URI }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}

      - name: Frontend - Install, Test and Build
        run: |
          cd frontend
          npm install
          npm test -- --watchAll=false --passWithNoTests
          npm run build
<<<<<<< Updated upstream
=======

  deploy:
    runs-on: self-hosted
    needs: build-and-test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Pull latest code
        run: |
          export PATH=/usr/local/bin:/usr/bin:/bin:$PATH
          cd ~/photo-gallery
          git pull origin main

      - name: Install dependencies
        run: |
          export PATH=/usr/local/bin:/usr/bin:/bin:$PATH
          cd ~/photo-gallery
          npm run install-all

      - name: Build frontend
        run: |
          export PATH=/usr/local/bin:/usr/bin:/bin:$PATH
          cd ~/photo-gallery/frontend
          npm run build

      - name: Restart backend with PM2
        run: |
          export PATH=/usr/local/bin:/usr/bin:/bin:$PATH
          pm2 restart photo-gallery || pm2 start ~/photo-gallery/backend/server.js --name photo-gallery
          pm2 save

>>>>>>> Stashed changes
